#include <bits/stdc++.h>
#define fio ios_base::sync_with_stdio(0), cin.tie(0)

using namespace std;

int main()
{
    fio;
    double a, b;
    cin >> a >> b;
    printf("%.10f", a/b);
    return 0;
}